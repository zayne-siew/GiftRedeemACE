{
    "name": "client",
    "version": "1.0.0",
    "description": "",
    "main": "dist/client.js",
    "scripts": {
        "build": "rimraf dist && tsc",
        "preclient": "npm run build",
        "client": "cross-env NODE_ENV=development nodemon -q dist/client.js",
        "prestart": "npm run build",
        "start": "cross-env NODE_ENV=production node dist/client.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "Zayne Siew",
    "license": "ISC",
    "dependencies": {
        "@types/node": "^20.11.19",
        "cross-env": "^7.0.3",
        "dotenv": "^16.4.4",
        "rimraf": "^5.0.5",
        "tslog": "^4.9.2"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "nodemon": "^3.0.3"
    }
}
